<html lang="en" dir="ltr">
  <head>
    <meta charset="utf-8">
    <title>To Do list</title>
  </head>
  <body>

    <% if(kindOfDay == "Saturday" || kindOfDay == "Sunday") { %>
    <h1 style="color: purple" class=""><%= kindOfDay%> Todo List</h1>
    <% } else { %>
      <h1 style="color:blue" class=""><<%= kindOfDay%>Todo List/h1>
   <% } %>
   
  </body>
</html>